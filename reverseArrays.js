/**
 * @param {character[]} s
 * @return {void} Do not return anything, modify s in-place instead.
 */
var reverseString = function(s) {
    
    
    // need to understand 
   //let arr =  new Array();
    // for(let i=s.length-1;i>=0;i--){
    //     console.log(s[i]);
    //     arr.push(s[i]);
    //     console.log(arr);
    // }
    // return arr;
    return s.reverse(); // with inbuilt function
};
